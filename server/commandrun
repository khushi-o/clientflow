# Local dev notes (do not put real passwords or Atlas URIs here — use server/.env only)

# Example: ensure mongod is on PATH if using local MongoDB
# $env:Path += ";C:\path\to\mongodb\bin"
# mongod --dbpath C:\path\to\data\db

# From repo root:
# cd server && npm run dev
# cd client && npm run dev

# MONGO_URI: copy from Atlas or set in server/.env (never commit .env)
